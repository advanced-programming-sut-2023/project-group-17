package Model.Items;

import Model.User;

public class ArmorAndWeapon extends Item{
    public ArmorAndWeapon(String itemName, double cost, User owner) {
        super(itemName, cost, owner);
    }
}
