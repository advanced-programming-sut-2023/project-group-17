package Server.model.Buildings;

import Server.model.User;

public class SiegeTent extends Building{
    public SiegeTent(User owner, Building building, int x, int y) {
        super(owner, building, x, y);
    }
}
